<section{{ attributes }}>
  {{ title_prefix }}
  {% if label %}
    <header class="major">
      <h2{{ title_attributes }}>{{ label }}</h2>
    </header>
  {% endif %}
  {{ title_suffix }}
  {% block content %}
    {{ content }}
  {% endblock %}
</section>
